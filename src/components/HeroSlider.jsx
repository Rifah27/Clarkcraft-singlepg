import React from "react";
import "../styles/heroSlider.css";

const Hero = () => {
  return (
    <section
      className="hero"
      style={{ backgroundImage: `url(${heroImage})` }}
    >
  </section>
  );
};

export default Hero;
